{
  "name": "boutique-shop",
  "version": "1.0.0",
  "description": "Boutique Shop - Full Stack Application",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "start": "cd backend && bun run server.js",
    "dev": "cd backend && bun run server.js",
    "install-all": "bun install && cd backend && bun install && cd ../frontend && bun install"
  }
}
